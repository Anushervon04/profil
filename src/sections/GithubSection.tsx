import { ArrowUpRight, Github, GitBranch, GitCommitHorizontal } from 'lucide-react';
import { developer } from '@/data/developer';
import { SectionHeading } from '@/components/SectionHeading';

export function GithubSection() {
  return <section className="section-padding bg-elev"><div className="section-container grid gap-10 lg:grid-cols-[.8fr_1.2fr] lg:items-center"><SectionHeading eyebrow="Open work" title="Code is part of the conversation." description="Explore repositories, experiments, and the ideas that make it into the open. Connect your profile in the configuration to make this section live." /><div className="rounded-2xl border border-base bg-card p-6 sm:p-8"><div className="flex items-center justify-between"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-elev text-fg"><Github size={20} /></span><div><p className="text-sm font-semibold text-fg">GitHub profile</p><p className="text-xs text-muted">Repositories & activity</p></div></div>{developer.github ? <a href={developer.github} target="_blank" rel="noreferrer" aria-label="Open GitHub profile" className="text-muted transition-colors hover:text-accent-500"><ArrowUpRight size={18} /></a> : <span className="rounded-md bg-elev px-2 py-1 font-mono text-[10px] text-muted">link pending</span>}</div><div className="mt-8 grid grid-cols-2 gap-3"><div className="rounded-xl border border-base bg-elev p-4"><GitBranch size={16} className="text-accent-500" /><p className="mt-4 text-xs text-muted">Repositories</p><p className="mt-1 font-display text-xl font-semibold text-fg">—</p></div><div className="rounded-xl border border-base bg-elev p-4"><GitCommitHorizontal size={16} className="text-accent-500" /><p className="mt-4 text-xs text-muted">Activity</p><p className="mt-1 font-display text-xl font-semibold text-fg">—</p></div></div><div className="mt-5 rounded-lg border border-dashed border-base p-4 text-xs leading-5 text-muted">Connect a GitHub URL in one place to turn this into a live profile and repository showcase.</div></div></div></section>;
}
