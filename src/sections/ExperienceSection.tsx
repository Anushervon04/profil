import { BriefcaseBusiness } from 'lucide-react';
import { experience } from '@/data/experience';
import { SectionHeading } from '@/components/SectionHeading';

export function ExperienceSection() {
  return <section id="experience" className="section-padding"><div className="section-container grid gap-14 lg:grid-cols-[.65fr_1.35fr] lg:gap-24"><SectionHeading eyebrow="The journey" title="Experience that compounds." description="A place for the work that shaped how I think about products, systems, and collaboration." /><div className="space-y-0">{experience.map((item, index) => <article key={`${item.company}-${item.period}`} className="relative border-l border-base pb-10 pl-8 last:pb-0"><span className="absolute -left-[9px] top-0 flex h-4 w-4 items-center justify-center rounded-full border-2 border-accent-500 bg-base"><span className="h-1.5 w-1.5 rounded-full bg-accent-500" /></span><div className="flex flex-col justify-between gap-2 sm:flex-row sm:items-start"><div><p className="font-mono text-[10px] uppercase tracking-widest text-accent-500">{item.period}</p><h3 className="mt-3 font-display text-xl font-semibold text-fg">{item.role}</h3><p className="mt-1 text-sm text-muted">{item.company}</p></div>{index === 0 && <BriefcaseBusiness size={20} className="text-muted" strokeWidth={1.5} />}</div><p className="mt-5 max-w-2xl text-sm leading-7 text-muted">{item.description}</p><div className="mt-5 flex flex-wrap gap-2">{item.technologies.map((tech) => <span key={tech} className="rounded-md bg-elev px-2.5 py-1 text-[10px] text-muted">{tech}</span>)}</div></article>)}</div></div></section>;
}
